is_active=True
is_active=False

if is_active:
    user_info = "active"
else:
    user_info = "deactive"

print(user_info)


user_info = "active" if is_active else "deactive"
print(user_info)

user_index= []
